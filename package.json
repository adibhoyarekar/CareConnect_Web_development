{
  "name": "careconnect-vercel",
  "version": "1.0.0",
  "description": "A comprehensive clinic appointment management system for doctors, receptionists, and patients.",
  "scripts": {
    "dev": "concurrently \"npm:build -- --watch\" \"cd backend && npm install && npm run dev\"",
    "build": "mkdir -p dist && cp index.html dist/index.html && esbuild index.tsx --bundle --outfile=dist/index.js --jsx=automatic"
  },
  "dependencies": {
    "react": "^19.1.1",
    "react-dom": "^19.1.1",
    "framer-motion": "^11.2.11",
    "@google/genai": "^0.14.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "esbuild": "^0.23.0"
  }
}